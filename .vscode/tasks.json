{
	"version": "2.0.0",
	"options": {
		"env": {
			"buildFolder": "${workspaceFolder}/build"
		}
	},
	"tasks": [
		/* __build */ {
			"label": "__build",
			"type": "shell",

			"command": "clear;cmake",
            "args": [
                "--build",
                "${buildFolder}"
            ]
		/* __cfgre */ }, {
			"label": "__cfgre",
			"type": "shell",

			"command": "clear;cmake",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			
			"args": [
				".",
				"-B",
				"${buildFolder}",
				// declare any additional CMake variables here for testing
				"-DCMAKE_BUILD_TYPE=Debug",
				//"-DCMAKE_TOOLCHAIN_FILE=${workspaceFolder}/CMake/x86_64-w64-mingw32.cmake"
			]
		/* __clean */  }, {
			"label": "__clean",
			"type": "shell",

			"command": "rm",
			"args": [
				"-rf",
				"${buildFolder}"
			]
		},
		
		/* build project */ {
			"label": "build project",
			
			"dependsOrder": "sequence",
			"dependsOn": [
				"__build"
			],

            "group": {
                "kind": "build",
                "isDefault": true
            }
		/* configure project */ }, {
			"label": "configure project",
			
			"dependsOrder": "sequence",
			"dependsOn": [
				"__cfgre"
			]
		/* clean and rebuild project */ }, {
			"label": "clean and rebuild project",
			
			"dependsOrder": "sequence",
			"dependsOn": [
				"__clean",
				"__cfgre",
				"__build"
			]
		/* clean and configure project */ }, {
			"label": "clean and configure project",
			
			"dependsOrder": "sequence",
			"dependsOn": [
				"__clean",
				"__cfgre"
			]
		}
	]
}